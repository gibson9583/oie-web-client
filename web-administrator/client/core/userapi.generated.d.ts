/* Hand-written declaration for the GENERATED userapi.generated.js (see
   tools/gen-userapi.mjs) — the generated module stays plain JS because it is a
   single 130KB string constant; a .ts twin would only double it on disk. */

/** The engine User API (userutil) surfaced as a .d.ts string for the Monaco
    TypeScript worker's extra-lib list. */
export const USER_API_DTS: string;
