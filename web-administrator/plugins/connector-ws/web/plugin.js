/* Web Service connector — web admin plugin (ConnectorSettingsPanel equivalent).
   Panels live in the shared client connector library (/connectors/ws.js). */
export { register } from '/connectors/ws.js';
