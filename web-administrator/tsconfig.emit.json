{
  "//": "Emits the raw-served .js twins for the TypeScript core modules (run via tools/build-core.mjs). tsc rather than esbuild so comments survive into the served files — plugin authors read /core/*.js in the browser. Type declarations are a separate emit (tsconfig.types.json).",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": false
  },
  "include": ["client/core/*.ts"]
}
