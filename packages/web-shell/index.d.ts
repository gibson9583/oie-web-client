/*
 * Type definitions for @oie/web-shell — the Shell API. `platform` is the object
 * a plugin registers through (the web equivalent of the Swing ClientPlugin SPI);
 * `loadPlugins` is the bootstrap the shell itself calls.
 *
 * GENERATED from the TypeScript sources (the canonical
 * web-administrator/client/core/pkg-shell.ts barrel) into ./types/ by
 * `npm run gen:types -w oie-web-administrator` — do not edit ./types by hand.
 * The library members (`platform.api`/`ui`/`oie`/`columns`) are typed by the
 * same generated graph, so they stay in sync with the implementation.
 */
export * from './types/pkg-shell';
