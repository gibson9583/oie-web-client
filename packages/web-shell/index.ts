/*
 * @oie/web-shell — OIE web admin Shell API (the platform extension points plugins
 * register into + loadPlugins). Public surface: ../../web-administrator/client/core/pkg-shell.js. Runtime
 * resolves to the shell's /core/pkg-shell.js via the import map (the ONE registry).
 */
export * from '../../web-administrator/client/core/pkg-shell.js';
