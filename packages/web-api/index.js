/*
 * @oie/web-api — OIE engine REST client + model helpers for web-admin plugins.
 * Public surface defined once in the shell's served barrel (../../web-administrator/client/core/pkg-api.js);
 * the lib build bundles it to ./dist for distribution. At runtime in the web
 * admin, `@oie/web-api` resolves (via the import map) to the shell's loaded
 * /core/pkg-api.js — one shared instance.
 */
export { default } from '../../web-administrator/client/core/pkg-api.js';
export * from '../../web-administrator/client/core/pkg-api.js';
