/*
 * Type definitions for @oie/web-api — the engine REST client + model helpers.
 *
 * GENERATED from the TypeScript sources (the canonical
 * web-administrator/client/core/pkg-api.ts barrel) into ./types/ by
 * `npm run gen:types -w oie-web-administrator` — do not edit ./types by hand.
 * Model types still come from the engine's OpenAPI spec (./types/oie-schema.d.ts,
 * produced by gen-schema.mjs), so they track the engine's Java model; the wire
 * shapes (`WireChannel`, `XStreamList`, `XStreamElements`, ...) are declared in
 * the sources (core/wire-types.ts) and re-exported here.
 */
export * from './types/pkg-api';
export { default } from './types/pkg-api';
